This is a seed application for Play project
===========================================

TODO : 

* change application secret in application.conf
* change name (play-app-seed) in build.sbt and in application.conf
* change app name in prod-logger
* set DB name in evolutions
* set db config in application.conf
* set meta info in messages.conf
* update logger file for prod mode
* change the google analytics / mixpanel id in views/tags/analytics.scala.html


# React app

run in dev
```
npm start
```

build for prod
```
npm run build
```

run test
```
npm test
npm test -- --coverage
```

on jenkins, CI env variable must be used
```
CI=true npm run build
CI=true npm test
```
